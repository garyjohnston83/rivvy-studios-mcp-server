# Public URL of your MCP HTTP server (used in docs, optional)
MCP_PUBLIC_URL=https://mcp.example.com
# Spring backend base URL (dev can be http://localhost:8080)
STUDIO_API_BASE=http://localhost:8080
# API key the MCP uses when calling Spring (Authorization: Bearer ...)
STUDIO_API_KEY=replace-me
# Token your MCP expects from the LLM host (OpenAI → MCP). Rotate per env.
INBOUND_MCP_TOKEN=replace-me
# Port for the MCP HTTP transport
PORT=3333